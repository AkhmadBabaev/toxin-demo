include ../card-info/card-info
include ../card-slider/card-slider

mixin card({ id, reviews = 0, price = 1000, stars = 1, images } = {})
  .card.js-card
    .js-card__images-gallery
      +card-slider({ images })

    .js-card__info
      +card-info({ id, reviews, price, stars })
