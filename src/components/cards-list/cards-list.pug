include ../card/card

mixin cards-list({ cards })
  .cards-list
    each card in cards
      .cards-list__card
        +card({
          id: card.id,
          price: card.price,
          rating: card.rating,
          reviews: card.reviews,
          type: card.type,
          images: card.images
        })
